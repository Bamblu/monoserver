services:
  - type: web
    name: bamblu-api
    runtime: node
    buildCommand: |
      corepack prepare yarn@4.5.3 --activate
      corepack yarn install --immutable
      cd apps/api
      npx prisma generate
      yarn build
    startCommand: corepack yarn workspace @bamblu/api start:prod